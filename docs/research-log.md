# Interval Edge Coloring Research Log

## 2026-08-22 — Phase A infrastructure

### Environment and policy

- All Python execution is now performed in WSL distribution `Ubuntu-22.04` at the user's request.
- WSL Python: 3.10.12; virtual environment: `.venv/`.
- Installed in the WSL environment: OR-Tools CP-SAT, NetworkX, MiniSat, Poppler, Nauty (`nauty-genbg`, `nauty-shortg`, etc.).
- Primary oracle: rank-potential CP-SAT model in `src/interval_edge_coloring.py`.
- Independent check: fixed-span start/color Boolean model over every span from `Delta(G)` through `n-1`.
- Every satisfiable solver answer is checked by an independent coloring verifier.
- A timeout is recorded as inconclusive and is never treated as non-colorability.

### Positive controls

All were classified colorable by the rank-potential model:

| graph | result | observed minimum span |
|---|---|---:|
| path on 10 vertices | colorable | 9 |
| complete bipartite `K(2,5)` | colorable | 6 |
| complete bipartite `K(3,6)` | colorable | 7 |
| random 3-regular bipartite graph on 20 vertices | colorable | 6 |
| random 5-regular bipartite graph on 20 vertices | colorable | 9 |

The `K(3,6)` result is a useful targeted control: it is not interval 6-colorable but has an interval 7-coloring.

### Published negative benchmarks reconstructed

Constructions were instantiated directly from Petrosyan-Khachatrian (2014), with source LaTeX preserved under `literature/raw/`.

| benchmark | order | size | maximum degree | primary exact result |
|---|---:|---:|---:|---|
| Malafiejski/Mirumyan rosette `Delta(5,5,5)` | 19 | 45 | 15 | UNSAT / non-colorable |
| Fano-plane construction `Erd(2,2,2,2,2,2,1)` | 21 | 52 | 13 | UNSAT / non-colorable |
| hat `K(3,4)` | 20 | 36 | 12 | UNSAT / non-colorable |
| hat `K(3,4)` minus one hub edge | 20 | 35 | 11 | UNSAT / non-colorable |
| hat `K(2,2,2)` | 19 | 36 | 12 | UNSAT / non-colorable |

CP-SAT status was `INFEASIBLE` for each. Runtime on this laptop was under 0.08 seconds per benchmark. The maximum-degree-11 deletion variant is the known boundary case that Lane 1 will mutate.

### Graph hashes (labelled canonical serialization)

These SHA-256 values identify the exact labelled JSON constructions generated by our code; isomorphism comparison remains a separate operation.

- `M5_delta_555`: `cc2407b775cc5092c2a98bd9b4b8311ef821233e310502cdb38430de94c98573`
- `Erd_Fano_2222221`: `35d345c1286cb6f888cd6d253dabc88156382a68931bff2e36173af6c44be506`
- `hat_K34`: `749d8229e6f3de0144e400f3ae5eb3b2e4919c558fe3feeebad378be74947184`
- `hat_K34_prime_Delta11`: `494ab07e878b2c06ea5915f6de150a34df9e37948558b4c24860830d88067af0`
- `hat_K222`: `f8dfda03aa4e8f6056f314c93229d6ed9aa7517fada2c10571d1853383c48ffd`

### Independent span checks

For every negative benchmark, the fixed-span Boolean encoding returned `INFEASIBLE` for each span from `Delta(G)` through `n-1`:

| benchmark | spans excluded |
|---|---|
| `M5_delta_555` | 15, 16, 17, 18 |
| `Erd_Fano_2222221` | 13 through 20 |
| `hat_K34` | 12 through 19 |
| `hat_K34_prime_Delta11` | 11 through 19 |
| `hat_K222` | 12 through 19 |

Full reports are in `results/phase-a-independent-spans.json`; graph JSON files are in `results/graphs/`.

### Weighted-hub observations

| benchmark | best hub | hub degree | weighted neighbor diameter | margin |
|---|---:|---:|---:|---:|
| `M5_delta_555` | v | 15 | 13 | +1 |
| `Erd_Fano_2222221` | u | 13 | 11 | +1 |
| `hat_K34` | u | 12 | 11 | 0 |
| `hat_K34_prime_Delta11` | u | 11 | 11 | -1 |
| `hat_K222` | u | 12 | 12 | -1 |

Thus the two high-degree families have a direct weighted-span obstruction. Hat `K(3,4)` lies exactly at equality; its degree-11 deletion needs interaction among multiple paths/ranks rather than the one-line diameter test. This supports the plan's choice to mutate the deletion variant in Lane 1.

The next subsection supersedes an earlier modular-relaxation claim caused by an encoding bug.

## Process audit — same day

The modular relaxation results in the preceding section are **invalidated**. The first implementation bounded the edge-potential difference to `[-(q-1), q-1]` before taking its residue. That is mathematically wrong because the unnormalized difference can be much larger. The code has been corrected to apply modulo reduction directly to the full linear expression. Modular rows must be regenerated before they are used as evidence.

Separately, too much time was spent attempting to construct Nauty graph input through `nauty-amtog`. That exploration did not alter any mathematical result, but it delayed canonicalization work and was not surfaced quickly enough. Canonicalization remains outstanding; NetworkX VF2 is currently available only for pairwise isomorphism checks.

Valid results unaffected by this audit:

1. all positive-control classifications;
2. rank-potential UNSAT classifications for all five negative benchmarks;
3. fixed-span INFEASIBLE results across every legal span for those benchmarks;
4. saved graph JSON and labelled hashes.

Immediate corrections to complete:

- rerun and overwrite modular reports using the corrected relaxation;
- test both a valid coloring and a deliberately corrupted coloring through the verifier;
- replace or wrap Nauty canonicalization with a tested graph6 pipeline.

### Correction results

After removing the incorrect intermediate bound, the corrected modulo-q relaxations are satisfiable for all five negative benchmarks at `q = 2, 3, 5`. Therefore none of these published examples has a simple residue obstruction at those moduli. Corrected primary results are in `results/phase-a-primary.corrected.json`; the earlier `results/phase-a-primary.json` must not be cited for modular evidence.

The verifier corruption test passed as intended:

- a CP-SAT path coloring verified as valid;
- incrementing one edge color was rejected because the global used-color set stopped being exactly `1..t`.

Remaining before Lane 1: implement a tested Nauty graph6 canonicalization path or use an equivalent bipartition-aware canonical form.

### Current status and next actions

Phase A's first exit condition is satisfied for the selected controls: positive controls and all four published negative families classify correctly. Remaining Phase A hardening:

1. ~~Run the independent fixed-span encoding across every legal span of each negative benchmark.~~ Complete.
2. ~~Save graph JSON, primary-oracle results, weighted-hub statistics, and modular relaxation outcomes in `results/`.~~ Complete; modular evidence must come from the corrected report.
3. ~~Add deliberate corruption tests to confirm the verifier rejects invalid certificates.~~ Complete.
4. ~~Add isomorphism-aware canonicalization using Nauty before mutation searches.~~ Complete.

Canonicalization implementation note: the packaged `nauty-labelg` command did not give stable partition-preserving output for our graph6 inputs, so the code now uses the Ubuntu `python3-pynauty` binding directly. It creates a Nauty colored graph with one color class per bipartition side and stores SHA-256 of Nauty's certificate in graph JSON. This was tested on three independent bipartition-preserving relabelings of the degree-11 benchmark (all certificates equal) and on a separate non-isomorphic pair (certificates differ).

## 2026-08-22 — Lane 1, first controlled searches

All searches below used the reconstructed 20-vertex, 35-edge, maximum-degree-11 benchmark `hat_K34_prime_Delta11` as parent. Candidate generation used bipartition-colored Nauty certificates for exact isomorph rejection. Primary classification used the rank-potential CP-SAT oracle with a 20-second limit and no timeouts.

| mutation class | definition | unique candidates after filtering | colorable | non-colorable |
|---|---|---:|---:|---:|
| hub-edge subsets | retain any 8, 9, 10, or 11 of the seed's 11 hub adjacencies; require connectedness and minimum degree 2 | 21 | 20 | 1 |
| degree-preserving switches | all non-isomorphic graphs reachable by one or two sequential bipartite 2-switches from the seed | 222 | 221 | 1 |
| core-margin redistribution | exhaustive connector multiplicity matrices on the `K(3,4)` core with every left-core row total 4 and right-core column total 3, plus choice of the single unhubbed connector; final maximum degree capped at 11 | 29 | 28 | 1 |

In each class, the sole negative has canonical hash `8:12:2a0fa9f624188ea6a9e03a62e6ff02679de6c7610136792b2bb06e8d44a348bd`, exactly the published seed. Thus these restricted Lane 1 neighborhoods contain **no new interval-non-colorable graph**.

### Minimality of the published seed

The exact rank-potential oracle checked every deletion:

- 35/35 single-edge deletions are colorable;
- 20/20 single-vertex deletions are colorable;
- no solve timed out.

Therefore the reconstructed known degree-11 benchmark is both edge-minimal and vertex-minimal. Full row-level results are in `results/known-seed-minimality.json`. Search reports are in:

- `results/lane1-hub-subsets.json`;
- `results/lane1-switch-depth2.json`;
- `results/lane1-redistribution.json`.

This is a useful negative boundary result, but it is not a discovery claim. The next step is to leave the fixed core-margin class while keeping either maximum degree at most 10 or order at most 18, as specified in Lane 2.

## 2026-08-22 — Lane 2 partial-hub scan

Generated every connected simple graph obtained by:

1. choosing a connected core `H` with minimum degree at least 2;
2. subdividing each edge once;
3. adding one hub adjacent to at least four subdivision vertices;
4. imposing final order at most 18, final maximum degree at most 10, and final minimum degree at least 2.

Core orders 4 through 9 were generated with Nauty `geng`; all hub-adjacency subsets of qualifying size were generated; duplicates were removed using the bipartition-colored Nauty certificate.

There were **9,031 unique candidates** after filtering and canonical rejection. Their weighted-hub margins relative to the added hub had this distribution:

| margin | count |
|---:|---:|
| -2 | 2 |
| -3 | 45 |
| -4 | 326 |
| -5 | 1115 |
| -6 | 2500 |
| -7 | 2946 |
| -8 | 1572 |
| -9 | 432 |
| -10 | 85 |
| -11 | 8 |

All 9,031 candidates were then classified exactly by the rank-potential oracle:

- colorable: 9031;
- non-colorable: 0;
- timeout/inconclusive: 0.

Total classification time was about 279 seconds. The complete checkpointed report is `results/lane2-partial-hub-exhaustive.json`. Thus the specified elementary one-hub, full-subdivision family contains no counterexample under these record-bound caps. A new attack needs either terminal synchronization gadgets (Lane 6), genuinely irregular connector degrees/multiplicities outside the core-margin class, or targeted small-order search beyond one-hub subdivisions.

## 2026-08-22 — Lane 6, first synchronization attempt

Replaced the seed's single degree-11 hub by two hubs `U0,U1`, partitioning the eleven connector adjacencies between them. Added one new subdivision vertex `W` adjacent to both hubs as the only direct synchronizer. This preserves the connector-core incidences and gives final order 21.

Searched all masks with both hub degrees between 4 and 9 (final maximum degree at most 9), then relaxed the cap to maximum degree 10:

| final degree cap | unique graphs | colorable | non-colorable | timeout |
|---:|---:|---:|---:|---:|
| 9 | 36 | 36 | 0 | 0 |
| 10 | 42 | 42 | 0 | 0 |

Reports are `results/lane6-split-hub.json` and `results/lane6-split-hub-delta10.json`. The simple two-way split with a single common subdivision edge does not preserve the effective high-degree span; it is too weak as a synchronization gadget. A useful next version needs a terminal gadget that forces the two hub palettes to concatenate rather than merely overlap at one color.

A second synchronization experiment replaced the common subdivision edge by one, two, or three internally disjoint length-4 paths `U0-S0-T-S1-U1`. All masks with both hub degrees at least 4 and final maximum degree at most 10 were tested.

There were 73 non-isomorphic candidates. All were colorable and no solve timed out. Report: `results/lane6-chained-sync.json`.

The combined Lane 6 evidence indicates that splitting the seed hub destroys the obstruction locally rather than merely failing to synchronize the two halves exactly. Future gadget work should first compute finite terminal signatures for small colorable synchronizers, then search for a composition that forces palette concatenation; blindly adding longer paths is unlikely to help.

## 2026-08-22 — New verified 19-vertex counterexamples

The next search applied same-side vertex identifications to the known degree-11 benchmark `hat_K34_prime_Delta11`. All candidates remained simple bipartite graphs: duplicate edges caused by identification were collapsed. Candidates were deduplicated with the colored Nauty certificate and classified by the rank-potential oracle.

| order reduction | target order | unique candidates | colorable | non-colorable | timeouts |
|---:|---:|---:|---:|---:|---:|
| one pair merge | 19 | 25 | 23 | **2** | 0 |
| reduction by two | 18 | 412 | 412 | 0 | 0 |
| reduction by three | 17 | 5,066 | 5,066 | 0 | 0 |

The two negative 19-vertex graphs are new relative to the reconstructed published benchmarks and non-isomorphic to each other.

### Q1-00012

- Constructed by identifying connectors `C0` and `C1`.
- Order 19, size 34, maximum degree 11, minimum degree 3; bipartition sizes 8 and 11.
- Canonical hash `8:11:eec834587010499acfe094698e6a75c05984de65c5a3d4e51289b2502aa75887`.
- Automorphism group order 8; 9 vertex orbits.

### Q1-00014

- Constructed by identifying connectors `C0` and `C4`.
- Order 19, size 34, maximum degree 11, minimum degree 2; bipartition sizes 8 and 11.
- Canonical hash `8:11:2412ce4d084d92cdaa41c7e1a009e8011de4be9e281a3fe6349eb7fc057ef9a0`.
- Automorphism group order 12; 9 vertex orbits.

For both graphs, every span from 11 through 18 was excluded. Verification used:

1. rank-potential CP-SAT;
2. an independent fixed-start/fixed-color CP-SAT encoding;
3. MiniSat on exported DIMACS CNFs;
4. PicoSAT-generated DRAT proofs checked by DRAT-Trim (`s VERIFIED` for all 16 span instances).

All single-edge and single-vertex deletions were checked exactly. Every deletion is colorable and no deletion solve timed out, so both graphs are edge-minimal and vertex-minimal under these checks. Controls included `K(3,5)`, which MiniSat proves UNSAT at spans 5 and 6 and SAT at span 7.

A same-day arXiv/OpenAlex literature recheck found no direct newer hit for interval-non-colorable bipartite graphs beyond the documented baseline. The novelty claim is deliberately narrow: new graphs relative to the five reconstructed benchmarks and the 2026-08-22 literature snapshot. They do not improve the known order or degree records.

Full certificate bundles:

- `results/candidates/Q1-00012/`
- `results/candidates/Q1-00014/`

A concise report is in `DISCOVERIES.md`.

Final bundle audit passed for both candidates: all stored graph invariants and canonical hashes agree; every legal span has CP-SAT infeasibility, MiniSat UNSAT, and checked DRAT verification; minimality reports have zero negatives/timeouts; novelty comparisons are false to every reconstructed benchmark; and all non-manifest bundle files match their listed hashes.

After those checks pass, begin Lane 1 mutations of `hat_K34_prime_Delta11`, preserving exact reclassification and independent span checks for every apparent negative.

## 2026-08-23 — Small-order exhaustive extension

While resuming the order-10 search, a graph6 round-trip test against NetworkX exposed an indexing error in the local encoder/decoder: it read the upper triangle row by row, whereas graph6 specifies destination-major order. Both directions were corrected and validated on 1,400 random graphs. Earlier conclusions based on the broken decoder were discarded.

The repaired pipeline then classified every side-size split for orders 10 through 13, always requiring connectivity and minimum degree at least 2. The rank-potential CP-SAT oracle produced:

| order | generated | nonregular solved | regular skipped | negatives | timeouts |
|---:|---:|---:|---:|---:|---:|
| 10 | 1,455 | 1,450 | 5 | 0 | 0 |
| 11 | 6,956 | 6,949 | 7 | 0 | 0 |
| 12 | 102,657 | 102,644 | 13 | 0 | 0 |
| 13 | 830,392 | 830,392 | 0 | 0 | 0 |

Nauty confirmed that processed rows are pairwise distinct within each bipartition-size class; different part-size pairs cannot be isomorphic for a connected bipartite graph except by interchange of the two parts, which the enumeration already identifies. The larger classes used deterministic residue slices on YSU `research_cpu`, with four CPU slots per slice. The order-13, 6+7 class had 128 slices; its slowest primary solve was 1.38 seconds under the two-second limit.

Thus no minimum-degree-2 connected counterexample exists on 10--13 vertices. The grouped cross-class audit is `results/all-gap-audits.txt`, earlier class audits remain in place, and the reusable runner is `src/search_small_bipartite.py`.

## 2026-08-23 — Complete order-14 exhaustive search

The same canonical pipeline was extended to every side-size split of order 14. Nauty generated 20,657,796 connected minimum-degree-2 bipartite graphs:

| split | generated | outcome |
|---|---:|---|
| 2+12 | 1 | colorable |
| 3+11 | 80 | all colorable |
| 4+10 | 8,670 | all colorable |
| 5+9 | 453,650 | all colorable |
| 6+8 | 5,916,568 | 5,916,567 colorable; one sweep timeout reran colorable |
| 7+7 | 14,278,827 | 14,278,788 solved colorable; 38 regular skipped; one sweep timeout reran colorable |

The 6+8 and 7+7 runs used 256 and 512 deterministic nauty-residue slices, respectively, on YSU `research_cpu` with four CPU slots per slice. Streaming audits confirmed exact index coverage and distinct bipartition-colored canonical hashes for every class. The only two two-second sweep timeouts were rerun with one-hour limits: the 6+8 record became colorable at span 11 in 1.63 seconds, and the 7+7 record became colorable at span 8 in 1.18 seconds. Thus there are no unresolved timeouts and no counterexample of order 14 in this scope.

Five parallel background agents also contributed: two monitored/audited the large classes, one built a reusable one-command independent verification workflow (`src/verify_candidate_workflow.py`), one exhausted a bounded Δ≤10 terminal-signature family (476 unique graphs, all colorable), and one counted the order-15 census at 288,643,968 graphs. The signature search also found that `src/lane6_chained_sync_search.py` used 11 connector bits although the reconstructed seed has 12 connectors; its saved result is complete only for the unintended 11-connector subfamily.

Combined with the previous run, all connected minimum-degree-2 bipartite graphs on 10--14 vertices are now classified: 21,599,256 canonical candidates, 21,599,218 nonregular solves, 38 regular skips, zero negatives, and zero unresolved timeouts.

The corrected 12-connector chained-synchronization search produced exactly the same 73 unique graphs as the earlier 11-bit run; the omitted high-index bit was never viable under the degree and connectivity filters. The corrected artifact is `results/lane6-chained-sync-corrected.json`.

Two bounded terminal-signature searches were also rerun to save reproducible artifacts. The strict endpoint-maximal filter yielded 280 unique Δ≤10 graphs; the broader forced-adjacent-offset filter yielded 196. All 476 were colorable with no timeouts. Reports are `results/lane6-signature-strict.json` and `results/lane6-signature-forced.json`.

## 2026-08-23 — Targeted order 16 and transformation lanes

The quarantined partial `5+11` order-16 file ended cleanly after 5,158,975 valid graph6 records. Regeneration with `genbg -c -g -l -d2:2 -D11:11` produced the same count into `results/targeted-order16/order16-5x11-d2to11.g6`. Since `genbg` lacks a native nonregular filter, nonregularity was enforced during classification. From the final 5,000-record tail, the 100 unique candidates with largest weighted-hub margin were solved exactly: 100 colorable, 0 non-colorable, 0 timeouts. This establishes only the stated bounded probe, not an exhaustive order-16 result.

The structural analyzer processed 3 verified negatives and 6,964 unique colorable near misses. After removing 13 duplicate source rows, it reconstructed 6,761 full near-miss graphs with exact hash agreement. A bounded solver audit rechecked 48 reports without mismatch or timeout. The strongest broadly applicable rule was `hub_best_margin >= -1.5`: it captured every verified negative with 31 false positives. Boundary false-positive rates were 9/5,478 for order at most 18 and 18/6,528 for degree at most 10. Artifacts are `results/structural-obstruction-analysis.json` and `results/structural-obstruction-findings.json`.

The degree-transfer search rewired rooted auxiliary gadgets around the verified Q1 examples and reconstructed benchmarks, enforcing connectivity, minimum degree 2, bipartiteness, and final maximum degree at most 10. It made 88,686 replacement attempts, retained 3,495 unique canonical graphs, and classified them all as colorable with zero timeouts. Erdős–Fano and the Δ15 benchmark reached explicit 1,500-unique caps; the other five parents exhausted their configured families. The best observed near-miss reached span 20 on the Δ15 benchmark. Report: `results/degree-transfer-delta10.json`.

Lane 4 formulated block-set systems as bipartite incidence graphs. Pairwise-intersecting blocks determine core adjacencies, while integer weights add private degree-2 connector paths between chosen block pairs. The configured family generated 372,570 candidates. Prioritized classification covered 743 unique graphs after 757 duplicates; all were colorable, with zero timeouts and maximum degree at most 10. Generation exhausted its configured space, classification exhausted its priority queue, and the run finished in 3,960 seconds. Report: `results/set-system-delta10.json`.

A subdivision audit exposed two defects in the earlier Δ≤10 attempt: cap and maximum-degree arguments had been interchanged, allowing an unmodified Δ15 parent to be saved, and the reduction test ignored the basic invariant that subdividing edge `xy` does not change either endpoint's degree regardless of path length. The invalid artifact was removed. Under the corrected definition, every legal candidate set for parents with maximum degree above 10 is empty; counts are 0 generated, 0 classified, 0 negatives, and 0 timeouts. Report: `results/subdivision-delta10-audited.json`. Future degree-reduction work uses same-side vertex splitting rather than ordinary subdivision.

## 2026-08-23 — Same-side splits and structured order-18 probes

The first corrected degree-reduction lane replaced an over-cap vertex by multiple vertices in the same bipartition side and deterministically redistributed incident edges. Every replacement had degree at least 2; duplicate edges were collapsed, and final graphs had to remain simple, connected, bipartite, and Δ≤10. The bounded family produced 56 unique candidates from seven seed roots. All 56 primary CP-SAT classifications were colorable with no timeouts, so no independent fixed-span confirmation was needed. Reports are `results/vertex-split-delta10-smoke.json` and `results/vertex-split-delta10.json`.

A separate structured order-18 search combined same-side identifications, bounded edge rewiring, low-degree deletion filtering, and reverse extension around known negatives. It generated 123,506 labeled candidates; 12,983 were unique by bipartition-colored Nauty certificate. Structural ranking selected the strongest 64 for exact classification. All were colorable with zero timeouts, and each selected row was reloaded to verify connectivity, minimum degree, order, and certificate agreement. This is a prioritized bounded probe rather than an exhaustive order-18 proof. Report: `results/order18-targeted-v2.json`.

The interrupted degree-transfer extension was resumed from its append-only state rather than discarded. For Erdős–Fano it skipped the baseline's first 1,900 signatures, generated 89,207 further replacements, and retained 1,256 new unique canonical graphs beyond the old cap. Including 244 rows recovered from the interrupted state, 1,500 extension candidates for that parent have now been classified exactly: all colorable, no timeouts, and no unresolved independent confirmations. The analogous extension of `M5_delta_555` remains in progress. Authoritative continuation artifacts are `results/degree-transfer-delta10-extension-resumed.json` and its state log.

The resumed degree-transfer extension subsequently completed `M5_delta_555`: it skipped the baseline's first 1,500 signatures, generated 109,747 replacements, retained exactly 1,500 new unique graphs, and classified them all as colorable without timeout. The complete resume processed 198,954 constructions and added 2,756 newly solved rows to 244 recovered rows. Thus the two capped parents contributed 3,000 unique extension classifications, all colorable. Maximum observed primary solve time was 0.397 seconds. The baseline and completed extension now represent 6,495 distinct colorable Δ≤10 graphs.

The targeted order-16 probe was extended to the final 20,000-record tail. All records were eligible and pairwise distinct by canonical certificate; none overlapped the original top-100 indices. The strongest 50 candidates were classified exactly by rank-potential CP-SAT: 50 colorable, 0 non-colorable, 0 timeouts. Every returned coloring passed internal verification. Report: `results/targeted-order16/tail-expansion-v2.json`.

The order-16 tail was expanded further to exactly 100,000 final records. All were eligible and unique by bipartition-colored Nauty certificate. Excluding the 150 prior indices, the top 500 fresh candidates were ranked by weighted-hub margin and solved exactly: 500 colorable, zero non-colorable, zero timeouts, and every returned coloring verified. Report: `results/targeted-order16/expansion-v3-summary.json`.

The deeper same-side splitting lane initially exposed two implementation defects: an uninitialized negative-event checkpoint path and, more importantly, selection of the unchanged high-degree seed when construction hit its attempt cap before returning valid splits. The cap-return bug was fixed so partial enumeration returns only generated valid successors. The repaired bounded run then generated 440 constructions, retained 224 unique Δ≤10 graphs, and classified every one as colorable without timeout. Report: `results/vertex-split-delta10-v2.json`; the invalid earlier smoke report is retained separately as `results/vertex-split-delta10-v2-smoke.json`.

The degree-transfer r3 pass also survived a router outage. Its durable state contains 1,957 exact rows: 740 Erdős–Fano and 1,217 M5 classifications, all colorable with no timeouts. A local continuation process remains active for the remaining configured M5 quota; it is not counted until its report reaches a terminal state.

That continuation completed after the failed agent turn. The r3 report records 177,021 further constructions, 5,152 newly solved unique canonical graphs, and zero non-colorable decisions or timeouts. Erdős–Fano contributed 740 classifications; `M5_delta_555` contributed 4,412. Combined with the baseline (3,495) and completed resumed extension (3,000), the represented total is now 11,647 unique colorable Δ≤10 graphs. Report and durable row log are `results/degree-transfer-delta10-extension-r3.json` and `results/degree-transfer-delta10-extension-r3-state.jsonl`.

## 2026-08-25 — Full order-16 attack and synchronized multi-hub splits

The full order-16 census was measured for every connected minimum-degree-2 side split with degrees at most 11:

| split | generated |
|---|---:|
| `4+12` | 26,330 |
| `5+11` | 5,158,975 |
| `6+10` | 291,917,907 |
| `7+9` | 121,471,162 |
| `8+8` | 159,757,218 |

The totals for the last two rows were initially underestimated because they came from timed-out local enumeration pipes. The authoritative `7+9` generation total is 3,604,370,591 records. The `8+8` total will be recorded only after its cluster generator completes.

`4+12` was classified in 32 Slurm slices and audited: 26,330 rows, all distinct by index and bipartition-colored hash, all colorable, no timeouts. `5+11` used 256 slices; its 5,158,975 rows had the same clean integrity properties and all were colorable. These audits establish the first two order-16 classes exhaustively.

`6+10` was generated on YSU into a 6.0 GB graph6 file with exactly 291,917,907 records. Classification array 228788 uses 512 contiguous chunks, four CPU slots per task, a ten-second primary limit per record, and up to 150 concurrent tasks. Results are checkpointed as append-only JSONL so interrupted tasks resume without duplicating indices. Any timeout will be rerun exactly with a one-hour limit; any primary negative must pass independent fixed-span confirmation before being reported.

Generation jobs for `7+9` (expected 121,471,162) and `8+8` (expected 159,757,218) are also running. Their workers verify exact line counts before launching analogous resumable arrays. A reusable remote auditor (`src/audit_order16_class.py`) streams chunks once, uses bounded snapshots and remote sorting, and reports malformed rows, duplicate indices/hashes, status histograms, missing ranges, timeouts, and negatives. It reproduced the committed `4+12` and `5+11` audits exactly.

## 2026-08-25 — Corrected 7+9 census and staged classification

The cluster generator for order-16 `7+9` completed in 2 hours 38 minutes and emitted **3,604,370,591** graph6 records, not the earlier partial estimate of 121,471,162. The file is 79,296,153,002 bytes and has SHA256 `902e940b2af30043d2a757e2825c0b274a2dd769465313eba533e66fe2dc5270`. This correction is important: dense side splits have much longer graph6 lines, so row-count estimates extrapolated from sparse classes are invalid.

Because the dataset is very large, classification is staged rather than submitted as one 8,192-task array. Batch 0 is Slurm array `228989`, covering chunks 0--999 (approximately the first 440 million graphs), with at most 150 tasks running. Each task uses four CPU slots, a ten-second primary CP-SAT limit per graph, and a 72-hour walltime. Results remain append-only and resumable by index. Later batches will cover the remaining chunk ranges after auditing throughput and integrity.

The synchronized multi-hub Δ≤10 lane replaced each over-cap hub by two same-side copies and inserted deterministic bipartite synchronization gadgets between them. It enumerated 2,388 constructions, retained 1,491 unique canonical graphs after enforcing connectivity, minimum degree 2, simplicity, and final Δ≤10, and classified all of them exactly. All 1,491 were colorable; there were no primary negatives, confirmed negatives, or timeouts. Reports are `results/multihub-sync-delta10-smoke.json` and `results/multihub-sync-delta10.json`.

A separate one-pass live snapshot audit ran on YSU `research_cpu` as job 227326, using about 8.4 GB peak memory in 19 minutes. It scanned 345 chunks and 129,324,159 result rows. There were no malformed JSON rows, missing fields, duplicate indices, or duplicate canonical hashes. The status histogram contained only colorable decisions, with zero timeout rows and zero negative rows. This validates the first 129,324,159 records of the expected 243,304,742-record `7+8` class; the remaining 113,980,583 records are still being generated.

## 2026-08-24 — Complete order-15 exhaustive search

All 512 slices of the final order-15 `7+8` class completed on YSU. The raw chunk files contained 243,290,613 newline-delimited primary results against an expected census of 243,304,742 records. A final one-pass audit (Slurm job 228009) used about 16.4 GiB peak memory and 30 minutes 37 seconds to scan all chunks once, externally sort indices and hashes, and verify schema coverage.

The audit found:

- 243,290,613 distinct zero-based indices;
- 243,290,613 distinct bipartition-colored canonical hashes;
- zero malformed JSON rows;
- zero missing required fields;
- zero duplicate index or hash rows;
- zero non-colorable classifications;
- three unresolved five-second sweep timeouts.

The apparent row deficit of 14,129 is fully explained by the runner's resume policy: when a timeout is later rerun successfully, it rewrites the original timeout row in place instead of appending a second row for the same index. Each of the three timeout indices was therefore rerun independently with rank-potential CP-SAT under a one-hour limit (Slurm job 227998):

| index | outcome | span | solve time |
|---:|---|---:|---:|
| 243,290,182 | colorable | 13 | 3.83 s |
| 243,304,543 | colorable | 12 | 4.40 s |
| 243,304,592 | colorable | 11 | 3.42 s |

After these exact resolutions, the effective histogram is 243,290,613 colorable, 0 non-colorable, and 0 timeout. Together with the previously audited small, 5+10, and 6+9 classes, all 288,643,968 connected minimum-degree-2 bipartite graphs on 15 vertices are now classified. There is no interval-non-colorable example on 15 or fewer vertices and no unresolved timeout anywhere in that exhaustive range.

Artifacts: `results/order15-7x8-final-audit.json`, plus `results/order15-7x8-timeout-243290182.json`, `results/order15-7x8-timeout-243304543.json`, and `results/order15-7x8-timeout-243304592.json`.

## 2026-08-25 — Five active research lanes

To keep progress visible while the large census jobs run, the root thread is coordinating five substantive workers. The `6+10` worker audits completed result chunks without treating a partial file as a completed chunk. The `7+9` worker validates the 8,192-chunk partition of the verified 3,604,370,591-record census and prepares conservative submission plans for later batches. The `8+8` worker tracks generator 229085 and prepares finalization/classification steps while explicitly rejecting the stale record estimate. The neighborhood-grafting worker continues exact classification and failure analysis for its construction family. A new degree-transfer worker is auditing the completed resumed extension before opening a fresh non-duplicative replacement family.

Scheduler checks found no failed or timed-out element in the active families: array 228788 had 120 running and 181 pending `6+10` elements, array 228989 had its first 1,000-task `7+9` batch queued with a 150-task throttle, and generator 229085 was running. The completed degree-transfer resume remains the latest negative construction result: 198,954 generated constructions, 2,756 newly classified unique graphs after recovery, all colorable, zero timeouts.

## 2026-08-25 — Reconciled transfer pilot and known-example reductions

The resumed degree-transfer extension was audited rather than accepted from its summary alone. Its 2,756 append-only resumed rows plus 244 recovered checkpoint rows form the 3,000-record completed report; all canonical hashes/signatures are unique, every graph passes the simple/connected/bipartite/minimum-degree/Δ checks, and a full primary-solver replay found no status mismatch. Thus baseline plus this extension represents 6,495 consistent colorable Δ≤10 certificates.

A fresh six-motif terminal-transfer pilot on `M5_delta_555` was seeded against the union of prior baseline, extension, and r3 certificates (11,647 hashes). It generated 15,077 constructions, rejected 14,077 as duplicates, and exactly classified 1,000 unique graphs: all colorable, zero timeouts, and zero seed overlap. All 1,000 stored rows were replayed for structural/hash agreement. The strongest new coloring span was 22. Reports are under `results/degree-transfer-delta10-agent/`.

A separate exhaustive first reduction pass around both known order-19 Δ11 examples generated vertex-only, Δ-cap edge-deletion, and combined families. After filtering invalid graphs and canonical deduplication, 48 unique survivors were classified exactly: all colorable, no timeouts, no disagreements, and no confirmed negatives. Artifacts are under `results/known-reduction-agent/`.

For `7+9`, the finished planning worker validated the exact 8,192-chunk partition and proposed later batches of at most 1,000 chunks at a 150-task throttle. Twelve sampled completed `6+10` chunks gave 133,053 rows/task-hour and 244.43 output bytes/record, implying roughly 3.3–6.6 hours for one 439,987-record `7+9` chunk and about 820 GiB of output for the full class. Root submitted three non-overlapping 2,000-record pilots on shards 307, 1536, and 2764 as Slurm jobs 229215--229217 to measure dense-end behavior. A dedicated snapshot auditor for `6+10` is Slurm job 229202. None of these jobs overlaps the main arrays.

The bounded known-witness reduction pass completed with 214 generated reductions and 48 canonical classes, all exactly colorable and none timed out. The retained distribution was 30 classes at order 18/Δ10, 8 at 18/Δ11, 8 at 19/Δ10, and 2 at 18/Δ4; thus every retained graph met the smaller-order or smaller-degree target, but none was interval-non-colorable.

An independent certification audit compared production rank-potential CP-SAT, production fixed-span CP-SAT, and a separately encoded PicoSAT CNF on 33 bipartite graphs over 262 legal spans. There were 135 SAT and 127 UNSAT decisions in each encoding, zero disagreements, zero unresolved cases, and all 135 SAT assignments validated as proper interval edge colorings. Three single-worker rank-potential reruns per case agreed. This supports the soundness of the current negative-candidate policy for the tested matrix.

The structured order-18 probe was extended to ranks 501--2500 of the same deterministic candidate queue. After an external process termination at row 1,832, the run was resumed from its append-only event stream for the exact missing ranks rather than regenerated. The authoritative reconciled report covers ranks 501--2500 exactly once: 2,000 unique canonical graphs, all colorable, with zero primary negatives, certified negatives, timeouts, or canonical overlap with the prior top-500 slice.

A broader multi-seed reduction pass combined Q1 witnesses with the M5, Erdős--Fano, and hat-family non-colorable seeds. It generated 17,542 reductions across cap-deletion/restoration, same-side identification, two-edge switching, and one/two-vertex deletion lanes. After global canonical deduplication, 1,025 unique target-satisfying graphs remained; the configured exact classification covered 1,000 of them. All were colorable, with zero timeouts and no primary negatives.

The first focused order-17 search generated 8,392 structured candidates from certified negative seeds, near-negative Q2 seeds, strong order-16 graphs, and ranked order-18 ancestors. It retained 2,351 unique canonical order-17 graphs and exactly classified the strongest 1,200: all colorable, zero timeouts, and no primary negatives. The remaining ranked candidates were queued for a disjoint continuation rather than recounted as completed.

That continuation completed ranks 1,201--2,351 after reconciliation against the v1 prefix: all 1,151 remaining canonical graphs were colorable, with zero timeouts and zero hash overlap. Thus the entire configured structured order-17 queue is now exhausted negative.

Because targeted coverage is not an exhaustive proof, work has started on an order-17 census feasibility plan across the eight bipartition-side classes. The plan will distinguish exact generated counts from estimates, verify graph6 files by line count and SHA256 before classification, and stage only classes whose storage/runtime budget is defensible on the YSU cluster.

The order-17 census plan now has exact structural counts for the three smallest classes: one graph at `2+15`, 144 at `3+14`, and 51,616 at `4+13`. The larger classes remain explicitly heuristic planning ranges; `5+12` is estimated at 15--30 million, while the two densest classes project tens of billions of records and are infeasible without separate capacity. Generation, physical sharding, resume-safe classification, audit, and independent negative-confirmation workflows were added for the tractable classes.

The small-class jobs are chained behind clean audits: `2+15` as Slurm jobs 229267--229272, `3+14` as 229273--229276, and `4+13` as 229277--229280 with 26 two-thousand-record classification shards. They use full simple-graph degree ceilings rather than the former Δ11 census restriction, and each generation must pass independent line-count, fixed-width, sample-validation, and SHA256 checks before publication.

The structured order-18 queue was extended through rank 4,500. Slice v5 covered ranks 2,501--4,500 exactly once after recovering from a transient checkpoint-process interruption; all 2,000 graphs were colorable, with no timeouts, duplicate ranks/hashes, or overlap with earlier slices. Cumulative targeted coverage is now 4,500 of 12,987 ranked unique graphs.

A near-miss mining pass compared high-span colorable constructions across transfer, split, order-17, and order-18 families. A bounded fresh envelope added 16 new exact classifications (all colorable) with zero collisions against 118,349 prior certificates. The strongest observed colorable span remains 22 for fresh M5 terminal-transfer motifs. The next construction hypothesis is therefore bridge coupling between terminal pairs, rather than more isolated terminal growth.

The first scaled bridge-coupling family generated 4,370 constructions on the five-spoke M5 skeleton. Canonical deduplication left 600 unique Δ≤10 graphs, all of which were exactly colorable with zero timeouts. Seven bridge-pair placements survived deduplication; the maximum primary span stayed at 22 and the median was 15. This negative result does not support further scaling of direct one-edge bridge coupling.

An expanded bridge-coupling pass enumerated all ten terminal pairs and every finite internal bridge port with round-robin coverage. It generated 21,090 constructions; 500 unique Δ≤10 survivors were classified exactly, all colorable with zero timeouts or overlap with the first bridge family. Its best span and hub margin matched the isolated-terminal benchmark, so direct bridge coupling is rejected as a scaling direction under this construction model.

Order-18 targeted slice v7 completed ranks 6,501--8,500 after its local process exited naturally. Reconciliation confirmed all 2,000 ranks exactly once, no duplicate/unexpected ranks, no rank/hash mismatches, and no overlap with slices v3--v6. All 2,000 graphs were colorable, with zero timeouts and therefore no independent confirmations to run.

A cumulative order-18 coverage ledger reconstructed the 12,987-rank queue and validated completed slices v3--v7 by rank/hash samples. It found exactly 8,500 authoritative covered ranks, no gaps/overlaps/duplicates/mismatches, plus 1,228 valid provisional v8 rows. Thus 9,728 ranks have been observed colorable and 3,259 remain unobserved.

Order-18 slice v8 subsequently completed all ranks 8,501--10,500. Reconciliation confirmed exact rank/hash coverage, no duplicate or missing rows, and no overlap with v3--v7. All 2,000 graphs were colorable with zero timeouts. The structured queue now has 10,500 authoritative covered ranks; only ranks 10,501--12,987 remain.

The neighborhood-graft lane was extended beyond its initial 94 selected survivors. A first continuation validated and skipped those solved rows, then classified the next 1,000 unique candidates: all colorable, bringing the lane total to 1,094 exact decisions with no negatives or timeouts.

The cumulative order-18 ledger was refreshed after v8 reconciliation: exactly ranks 1--10,500 are authoritatively covered, every covered graph is colorable, and only ranks 10,501--12,987 remain unobserved. Six deterministic rank/hash sample checks across v3--v8 passed, with no overlaps, gaps, duplicates, or hash mismatches.

The neighborhood-graft predecessor was identified as an interrupted checkpoint rather than an intentional one-root rerun and was preserved byte-for-byte. A corrected extension across all seven declared roots generated 61,375 constructions and retained 10,950 unique Δ≤10 graphs. Only three roots were eligible under the final-degree cap; their 94 selected high-margin survivors were classified exactly and all were colorable, with zero timeouts.

The cluster queue remained healthy but saturated by the per-user job limit. The `6+10` array continued advancing, while the bounded incremental audit and order-17 small-class chains stayed pending with their dependencies intact. Compact scheduler snapshots now record the audited 120-chunk prefix (68,418,360 clean rows) and the exact order-17 job/resource plan.

Order-16 `8+8` generation completed successfully in 5h14m. The stable output is 185,259,598,216 bytes, divisible by the independently sampled 22-byte record width, implying only a provisional count of 8,420,890,828 records. Exact line count and SHA256 remain gated behind successful finalizer job 229204; physical sharding and classification stay blocked until that manifest is validated.

Two audit gates were hardened while scheduler slots remain saturated: a synthetic-tested independent validator for the `8+8` finalization manifest/marker/hash/count/shard conditions, and stricter order-17 small-class acceptance checks for manifest cardinality, row schema/domain, canonical hashes, and exact shard counts. Neither downstream stage was released.

The order-17 auditor was further hardened against non-finite solver times and invalid spans, and its six fixture cases still pass. Compact snapshots continue to distinguish physically present `6+10` chunks from accepted audit scope: chunks 0--119 are accepted, while chunks 120--143 contain exactly 13,683,672 rows but await clean audit job 229252.

The `6+10` terminal-audit workflow was repaired to avoid rescanning unchanged chunks when the global hash sidecar is still incomplete. Chunks 0--119 are validated from cache with 68,418,360 sorted hash entries; incremental checkpoints and cache replacements are atomic, and per-pass versus cumulative scope is reported separately. Bounded Slurm job 229252 was submitted for the next 24 oldest terminal chunks using four CPU slots and 32 GB memory.

An independent order-18 spot audit rebuilt the deterministic queue and rechecked 18 rows from each completed slice v3--v8, totaling 108 claims. For every colorable claim it validated structure, canonical hash, rank-potential certificate, and fixed-span satisfiability at the reported span using a different worker count. It found zero invalid certificates, solver disagreements, or mismatches.

A second neighborhood-graft continuation preserved ranks1--1,094 and classified ranks1,095--2,094. All 1,000 new graphs were colorable with zero timeouts; the neighborhood-graft negative ledger now contains 2,094 exact decisions.

The alternate order-18 family reached another configured cap in v2: 12,812 constructions produced 2,231 globally unique candidates after excluding the first queue; 1,000 new graphs were classified exactly, all colorable, leaving 31 residuals at those bounds. A separate spot audit of alternate-family v1 reported canonical-hash/rank mismatches and is treated as unresolved until a field-by-field investigation distinguishes reconstruction-order defects from genuine record errors; it does not invalidate the original colorable decisions by itself.

After a WSL restart terminated local helper processes, all interrupted construction lanes were resumed from durable checkpoints rather than regenerated. The neighborhood-graft ledger was reconciled through rank5,094 with no gaps/duplicates/negatives/timeouts, and ranks5,095--6,094 were restarted under low priority. On YSU, the first real `7+9` classification element remained healthy past 330,000 of 439,987 rows, all colorable. The `6+10` strict acceptance job, `8+8` finalizer, and first order-17 generator remained queued behind the per-user job limit; none was canceled or duplicated. Their latest gate snapshots were written on the cluster, not yet mirrored into this repository.

The final structured order-18 tail completed ranks10,501--12,987. Its reconciliation found all 2,487 ranks exactly once, zero duplicate/missing/unexpected ranks, zero rank/hash mismatches, and zero rank or canonical-hash overlap with v3--v8. Every tail graph was colorable with zero timeouts. Together with slices v3--v8, the deterministic structured queue of 12,987 unique order-18 graphs is now exhaustively covered negative.

The finalized cumulative ledger confirms that all 12,987 structured order-18 candidates are covered exactly once, all canonical hashes are globally unique, every decision is colorable, and there are no gaps, overlaps, duplicates, rank/hash mismatches, or timeouts. Boundary/midpoint samples from all seven constituent slices passed. This is exhaustive coverage of the constructed structural queue, not an exhaustive census of all order-18 bipartite graphs.

An independent tail audit replayed 36 deterministic rows spanning ranks10,501--12,987 using two-worker rank-potential and fixed-span runs. All structural, hash, certificate, and solver checks passed with zero mismatches; maximum sampled runtime was 0.736 seconds.

The neighborhood-graft continuation was extended through rank3,094 after validating all prior rows without recomputation. The new contiguous batch contained 1,000 unique graphs, all colorable with zero timeouts; the lane now has 3,094 exact negative decisions.

An independent spot audit sampled 24 rows from each of the initial, beyond-top94, and beyond-top1094 graft ledgers (72 total). All structural, canonical-hash, decision/status, certificate, and fixed-reported-span checks passed. Two rows had nonminimal `reported_span` metadata: both ledger entries said span11, while independent serial replays produced verified span10 colorings. This is a reporting defect only; it does not affect their colorable decisions or the search's negative conclusion.

The neighborhood-graft continuation reached rank4,094. It validated all prior decisions without recomputation, then classified ranks3,095--4,094 exactly: all 1,000 were colorable with zero timeouts.

A second structured order-18 family used same-side identification with bounded edge deletion/restoration plus cyclic three-edge switches. It generated 6,904 candidates, retained 2,111 internally unique graphs, excluded 497 overlaps with the completed first queue, and classified the strongest 1,200 of its 1,614 globally new candidates. All were colorable with zero timeouts.
